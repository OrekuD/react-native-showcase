# Changelog

## 0.1.1

- Add MIT license and package metadata to the published CLI.
- Document CLI development, publishing, and release workflow.
- Normalize the executable path for npm publishing.

## 0.1.0

- Add the `button` registry entry.
- Add `rn-showcase add` with package-manager detection, safe overwrite
  handling, custom destinations, and dependency installation.
